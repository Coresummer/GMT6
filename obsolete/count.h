#pragma once
#ifndef COUNT_H
#define COUNT_H

#include "define.h"

void cost_zero();
void costcheck(&tmp_cost);
cost_printf();
float timedifference_msec(struct timeval tv_start, struct timeval tv_end);

#endif
